{
  "name": "outsidevisual-maintenance",
  "version": "1.0.0",
  "description": "Percy visual regression snapshots for client sites",
  "scripts": {
    "snap:royalmtLive": "npx percy exec -- node snapshots.js royalmtLive",
    "snap:royalmtSTG": "npx percy exec -- node snapshots.js royalmtSTG",
    "snap:lindauer": "npx percy exec -- node snapshots.js lindauer"
  },
  "dependencies": {
    "@percy/cli": "latest",
    "@percy/puppeteer": "latest",
    "puppeteer": "latest"
  }
}